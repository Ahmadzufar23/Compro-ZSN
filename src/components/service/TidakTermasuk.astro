---
/**
 * TidakTermasuk — strip ink, daftar eksklusi eksplisit. Sumber
 * sengketa nomor satu di kontrak adalah asumsi yang tidak pernah
 * ditulis -> ditulis eksplisit di sini.
 */
import Section from '@/components/ui/Section.astro';

interface Props {
  items: string[];
}
const { items } = Astro.props;
---

<Section bg="ink" size="sm" aria-label="Tidak termasuk">
  <p class="label-tech text-yellow">Tidak Termasuk</p>
  <ul class="mt-5 grid gap-x-8 gap-y-3 sm:grid-cols-2">
    {
      items.map((item) => (
        <li class="flex gap-3 text-sm text-white/70">
          <span class="mt-2 size-1 shrink-0 rounded-full bg-white/40" />
          {item}
        </li>
      ))
    }
  </ul>
</Section>
