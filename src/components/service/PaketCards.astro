---
/**
 * PaketCards — kartu paket. hargaMulai kosong -> "Minta Penawaran" +
 * tombol "Konsultasi" (bukan "Pilih Paket", bahasa e-commerce yang
 * tidak cocok untuk situs tanpa checkout).
 */
import Section from '@/components/ui/Section.astro';
import Button from '@/components/ui/Button.astro';

interface PaketItem {
  nama: string;
  ringkas: string;
  hargaMulai?: string;
  satuan?: string;
  durasi?: string;
  isi: (string | { label: string; nilai: string })[];
}

interface Props {
  items: PaketItem[];
}
const { items } = Astro.props;
---

<Section bg="mist" size="md" aria-label="Paket">
  <p class="label-tech text-amber-text">Paket</p>
  <h2 class="mt-3 text-h2">Pilihan Paket</h2>

  <div class="mt-10 grid gap-6 md:grid-cols-3">
    {
      items.map((p) => (
        <div class="on-dark flex flex-col rounded-md border-2 border-transparent bg-ink p-6 transition-colors hover:border-yellow focus-within:border-yellow">
          <h3 class="text-h3 text-white">{p.nama}</h3>
          <p class="mt-2 text-sm text-slate-muted">{p.ringkas}</p>

          <p class="mt-5 font-display text-2xl font-bold text-white">
            {p.hargaMulai ? (
              <>
                Mulai dari Rp {p.hargaMulai} juta
                {p.satuan && <span class="text-sm font-normal text-slate-muted">{p.satuan}</span>}
              </>
            ) : (
              'Minta Penawaran'
            )}
          </p>
          {p.durasi && <p class="label-tech mt-1 text-slate-muted">Estimasi durasi: {p.durasi}</p>}

          <ul class="mt-6 flex-1 space-y-2.5 border-t border-steel pt-5 text-sm">
            {p.isi.map((row) =>
              typeof row === 'string' ? (
                <li class="text-slate-muted">{row}</li>
              ) : (
                <li class="flex justify-between gap-4">
                  <span class="text-slate-muted">{row.label}</span>
                  <span class="font-medium text-white">{row.nilai}</span>
                </li>
              )
            )}
          </ul>

          <Button href="/kontak" variant="outline-light" class="mt-6">
            Konsultasi
          </Button>
        </div>
      ))
    }
  </div>
</Section>
